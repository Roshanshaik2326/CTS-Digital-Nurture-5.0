# Git HOL 3 Notes

## Branch

A branch is an independent line of development.

## Merge

Merging combines changes from one branch into another.

## Key Learning

- Create a branch using `git branch`.
- Switch branches using `git checkout`.
- Merge changes using `git merge`.
- Delete merged branches using `git branch -d`.